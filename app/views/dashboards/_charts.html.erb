<div id="network_graph" class="span-22 last" >
        <h3>Ventas últimos 12 meses</h3>
                <% high_chart ("year_sales", @year_sales)  do |c| %>
					<%= "options.chart.width = 700" %>
					<%= "options.chart.height = 300" %>
					<%= "options.tooltip.formatter = function() {return this.point.name +': '+ this.y}"%>
					<%= "options.legend.enabled = true " %>
					<%= "options.yAxis.title.text = 'Total en pesos' "%>
					
				<% end %>
        <hr/>
</div>